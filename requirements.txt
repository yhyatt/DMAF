# Core dependencies
PyYAML>=6.0
Pillow>=10.0.0
numpy>=1.24.0
requests>=2.28.0

# File watching
watchdog>=3.0.0

# Google Photos API
google-auth>=2.22.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0

# Face recognition backend #1: face_recognition (dlib-based, CPU-optimized)
face-recognition>=1.3.0
dlib>=19.24.0

# Face recognition backend #2: InsightFace (deep learning, more accurate)
insightface>=0.7.3
onnxruntime>=1.15.0

# Development & Testing (Phase C+)
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

# Code quality tools (Phase E)
ruff>=0.1.0
black>=23.0.0
mypy>=1.5.0
types-requests>=2.31.0
types-PyYAML>=6.0.0

# Cloud deployment (Phase F)
# Google Cloud SDK will be installed separately if needed
# rclone will be installed via system package manager
